import { CategoryTasksPipe } from './category-tasks.pipe';

describe('CategoryTasksPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryTasksPipe();
    expect(pipe).toBeTruthy();
  });
});
